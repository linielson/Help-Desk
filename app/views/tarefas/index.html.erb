<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "Lista", tarefas_path  %></li>
      <li><%= link_to "Novo", new_tarefa_path %></li>
      <li><%= link_to 'Imprimir', impressao_path %></li>
    </ul>
  </div>

  <div class="content">
    <h2 class="title">Tarefas</h2>

    <div class="inner">

      <table class="table">
        <tr>
          <th class="first">Código</th>
          <th>Técnico</th>
          <th>Projeto</th>
          <th>Cliente</th>
          <th>Emissão</th>
          <th>Entrega</th>
          <th>Solicitação</th>
          <th>Status</th>
          <th class="last">&nbsp;</th>
        </tr>

        <% @tarefas.each do |tarefa| %>
        <tr class="<%= cycle("odd", "even") %>">
          <td><%= tarefa.id %></td>
          <td><%= tarefa.tecnico.nome_completo.titleize %></td>
          <% if tarefa.projeto.present? %>
            <td><%= tarefa.projeto.nome.titleize %></td>
          <% else %>
            <td> </td>
          <% end %>
          <td><%= tarefa.cliente.nome.titleize %></td>
          <td><%= l tarefa.emissao %></td>
          <td><%= l tarefa.entrega %></td>
          <td><%= truncate tarefa.solicitacao, length: 40 %></td>
          <td style="width: 90px"><%= tarefa.status_nome %></td>

          <td class="last">
            <% if tarefa.aberta? %>
              <%= link_to image_tag("iniciar.png"), tarefa_iniciar_path(tarefa), confirm: 'Esta ação dará inicio a execução desta tarefa. Já verificou todas as etapas? Você realmente deseja prosseguir?' %>
            <% elsif tarefa.em_andamento? %>
              <%= image_tag("iniciar_d.png") %>
            <% else %>
              <%= image_tag("concluido.png") %>
            <% end %>

            <%= link_to image_tag("etapas.png"), tarefa_tarefa_etapas_path(tarefa) %>
            <%= link_to image_tag("horas.png"), tarefa_tarefa_horas_path(tarefa) %>
            <%= link_to image_tag("anexos.png"), tarefa_tarefa_anexos_path(tarefa) %>
            <%= link_to image_tag("consultar.png"), tarefa %>

            <% if tarefa.aberta? or tarefa.em_andamento? %>
              <%= link_to image_tag("editar.png"), edit_tarefa_path(tarefa) %>
              <%= link_to image_tag("excluir.png"), tarefa, confirm: 'Você realmente deseja excluir esta tarefa?', method: :delete %>
            <% else %>
              <%= image_tag("editar_d.png") %>
              <%= image_tag("excluir_d.png") %>
            <% end %>
              
          </td>
        </tr>
        <% end %>
        
      </table>

      <div class="actions-bar wat-cf">
        <div class="actions"> </div>
        <%= will_paginate @tarefas %>
      </div>

    </div>
  </div>
</div>

<%= render 'pesquisa' %>

<% content_for :sidebar, render(partial: 'sidebar') %>
