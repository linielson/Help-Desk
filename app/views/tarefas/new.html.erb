<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to "Lista", tarefas_path %></li>
      <li class="active"><%= link_to "Novo", new_tarefa_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title">Tarefa</h2>
    <div class="inner">
      <%= form_for :tarefa, url: tarefas_path, html: {class: :form} do |f| %>
        <%= render partial: "form", locals: {f: f} %>
      <% end %>
    </div>
  </div>
</div>

<% content_for :sidebar, render(partial: 'sidebar') %>
