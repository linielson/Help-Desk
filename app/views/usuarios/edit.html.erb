<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to "Lista", usuarios_path %></li>
      <li><%= link_to "Novo", new_usuario_path %></li>
      <li class="active"><%= link_to "Editar", edit_usuario_path %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title">Usuário</h2>
    <div class="inner">
      <%= form_for @usuario, url: usuario_path(@usuario), html: {class: :form} do |f| %>
        <%= render partial: "form", locals: {f: f} %>
      <% end %>
    </div>
  </div>
</div>

<% content_for :sidebar, render(partial: 'sidebar') %>
